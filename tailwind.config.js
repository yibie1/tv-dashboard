/** @type {import('tailwindcss').Config} */
// Tailwind config kept for reference — styles are applied via StyleSheet, not className
module.exports = {
  content: [],
  theme: { extend: {} },
  plugins: [],
};
